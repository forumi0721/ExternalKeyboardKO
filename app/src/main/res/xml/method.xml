<?xml version="1.0" encoding="utf-8"?>
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:isDefault="true"
    android:settingsActivity="kr.stonecold.exkeyko.SettingsActivity">

    <!-- Subtype 미사용 -->
    <subtype
        android:label="@string/subtype_generic"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:isAuxiliary="false"
        android:languageTag="en-US"
        android:imeSubtypeExtraValue="KeyboardLayoutSet=Qwerty" />

    <!-- 영어 키보드 서브타입 -->
    <!-- Subtype 쓸 경우
    <subtype
        android:label="@string/subtype_english"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard"
        android:isAuxiliary="false"
        android:languageTag="en-US"
        android:imeSubtypeExtraValue="KeyboardLayoutSet=Qwerty" />
    -->

    <!-- 한국어 키보드 서브타입 -->
    <!-- Subtype 쓸 경우
    <subtype
        android:label="@string/subtype_korean"
        android:imeSubtypeLocale="ko"
        android:imeSubtypeMode="keyboard"
        android:isAuxiliary="true"
        android:languageTag="ko-KR"
        android:imeSubtypeExtraValue="KeyboardLayoutSet=HangulQwerty" />
    -->
</input-method>
